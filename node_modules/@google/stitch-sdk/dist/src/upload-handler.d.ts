import type { StitchToolClientSpec } from './spec/client.js';
import { type UploadImageInput, type UploadImageResult, type UploadImageSpec } from './spec/upload.js';
/**
 * Handler for uploadImage — implements UploadImageSpec.
 *
 * Never throws. All failures are returned as UploadImageResult with a typed
 * error code. The caller (Project.uploadImage) surfaces failures as StitchError.
 */
export declare class UploadImageHandler implements UploadImageSpec {
    private readonly client;
    constructor(client: StitchToolClientSpec);
    execute(projectId: string, input: UploadImageInput): Promise<UploadImageResult>;
}
//# sourceMappingURL=upload-handler.d.ts.map